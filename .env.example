# Database (match docker-compose postgres)
DB_HOST=localhost
DB_PORT=5432
DB_USER=marketplace_user
DB_PASS=marketplace_pass
DB_NAME=marketplace_testtask

# JWT
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN_SEC=3600

# Server
PORT=3000
