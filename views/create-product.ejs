<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create Product | Marketplace Admin</title>
  <link rel="stylesheet" href="/dashboard.css">
</head>
<body>
  <header class="header">
    <a href="/dashboard" class="logo">Marketplace Admin</a>
    <nav class="nav">
      <span class="user-email"><%= user.email %></span>
      <a href="/dashboard/logout" class="btn btn-outline">Logout</a>
    </nav>
  </header>
  <main class="main">
    <h1>Create Product</h1>
    <a href="/dashboard" class="back-link">← Back to dashboard</a>

    <form method="POST" action="/dashboard/products" enctype="multipart/form-data" class="form card">
      <% if (typeof error === 'string' && error) { %>
        <div class="alert alert-error"><%= error %></div>
      <% } %>
      <div class="form-group">
        <label for="name">Name</label>
        <input type="text" id="name" name="name" value="<%= typeof body !== 'undefined' && body && body.name ? body.name : '' %>" required>
      </div>
      <div class="form-group">
        <label for="description">Description</label>
        <textarea id="description" name="description" rows="3"><%= typeof body !== 'undefined' && body && body.description ? body.description : '' %></textarea>
      </div>
      <div class="form-row">
        <div class="form-group">
          <label for="price">Price</label>
          <input type="number" id="price" name="price" step="0.01" min="0" value="<%= typeof body !== 'undefined' && body && body.price ? body.price : '' %>" required>
        </div>
        <div class="form-group">
          <label for="stock">Stock</label>
          <input type="number" id="stock" name="stock" min="0" value="<%= typeof body !== 'undefined' && body && body.stock ? body.stock : '' %>" required>
        </div>
      </div>

      <div class="form-group">
        <label for="files">Images (required, 1–5)</label>
        <input type="file" id="files" name="files" multiple accept="image/*" required>
        <p class="help">At least 1 image required. Maximum 5 images.</p>
      </div>

      <div class="form-actions">
        <button type="submit" class="btn btn-primary">Create product</button>
        <a href="/dashboard" class="btn btn-outline">Cancel</a>
      </div>
    </form>
  </main>
</body>
</html>
